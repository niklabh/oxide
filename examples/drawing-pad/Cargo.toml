[package]
name = "drawing-pad"
version = "0.1.0"
edition = "2021"
description = "Interactive drawing pad example for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
