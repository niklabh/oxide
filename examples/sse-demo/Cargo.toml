[package]
name = "sse-demo"
version = "0.1.0"
edition = "2021"
description = "Server-Sent Events demo for the Oxide browser"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
