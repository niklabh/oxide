[package]
name = "typography-demo"
version = "0.1.0"
edition = "2021"
description = "Typography showcase: font family, weight, style, alignment, and measurement"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
