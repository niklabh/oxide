[package]
name = "worker-demo-bg"
version = "0.1.0"
edition = "2021"
description = "Background worker demo for the Oxide browser (worker module)"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxide-sdk = { path = "../../oxide-sdk" }
